# Contributing

Preserve the canonical Quran checksum and do not edit verified religious resources without provenance, licence, integrity metadata, and dedicated review. Run `flutter pub get`, `flutter analyze`, and `flutter test --concurrency=1` before proposing changes. Never commit signing keys, credentials, backups, location data, or device logs containing personal state.
